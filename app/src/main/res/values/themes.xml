<resources>

    <style name="Base.Theme.CampusExpenseManager" parent="Theme.Material3Expressive.DynamicColors.Light" />

    <style name="Theme.CampusExpenseManager" parent="Base.Theme.CampusExpenseManager">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="splashScreenIconSize">48dp</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash</item>
        <item name="postSplashScreenTheme">@style/Theme.CampusExpenseManager</item>
    </style>
</resources>